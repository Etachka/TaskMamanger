<Window x:Class="TaskMamanger.Forms.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskMamanger.Forms"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid Margin="10">
            <ItemsControl Name="icTodoList" ItemsSource="{Binding TaskColumns}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="5" CornerRadius="5" Background="#F8F8F8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding Name}" FontWeight="Bold" FontSize="18" Margin="10" Foreground="DimGray" />
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding Tasks}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="10,5" Padding="10" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Background="White">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>

                                                        <TextBlock x:Name="_date" Grid.Row="0" Foreground="DimGray"
                                               Margin="0,0,0,5" Text="{Binding EndTime}" FontWeight="Bold"/>
                                                        <TextBlock x:Name="_name" Grid.Row="1" Foreground="DimGray"
                                               Text="{Binding Name}"/>
                                                        <StackPanel Orientation="Horizontal" Grid.Row="2">
                                                            <TextBlock x:Name="_id" Foreground="Gray"
                                                   Text="{Binding ID}"/>
                                                            <Ellipse Width="20" Height="20" Fill="HotPink" HorizontalAlignment="Right" Margin="10,0,0,0">
                                                                <Ellipse.ToolTip>
                                                                    <TextBlock Text="{Binding Picture}" Foreground="DimGray"/>
                                                                </Ellipse.ToolTip>
                                                            </Ellipse>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Grid>
</Window>
